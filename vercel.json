{
  "buildCommand": "cd artifacts/calcstride && npm install && npm run build",
  "outputDirectory": "artifacts/calcstride/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}