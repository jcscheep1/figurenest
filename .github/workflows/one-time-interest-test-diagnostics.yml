name: One-time Interest test diagnostics

on:
  push:
    branches:
      - fix/interest-intent-consolidation

permissions:
  contents: write

jobs:
  diagnose:
    if: ${{ !contains(github.event.head_commit.message, '[interest-test-diagnostics]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/interest-intent-consolidation
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec tsc -b lib/api-client-react
      - name: Capture unit-test output
        shell: bash
        run: |
          set +e
          pnpm --filter @workspace/calcstride test > interest-test-output.txt 2>&1
          status=$?
          echo "exit_status=$status" >> interest-test-output.txt
          rm .github/workflows/one-time-interest-test-diagnostics.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Capture Interest test diagnostics [interest-test-diagnostics]"
          git push origin HEAD:fix/interest-intent-consolidation
